# API bridge base URL (Cloudflare Worker — RF_09)
# Leave empty to use local demo data in FE
VITE_CBV_API_BASE_URL=http://localhost:8787

# Optional: test role against Worker auth stub (ADMIN|MANAGER|STAFF|FINANCE|HO_SO|VIEW_ONLY)
# VITE_CBV_ROLE=MANAGER
