/**
 * Retained module stub — RIGHT panel no longer mounts checklist step UI
 * (PHASE_CHECKLIST_RIGHT_PANEL_FULL_REMOVE_QUICK_SUMMARY).
 * Quick actions live in CENTER only; RIGHT uses Chi tiết / Timeline / Handoff / Hồ sơ tabs.
 */
export function FocusedChecklistStepDetailPanel(_props: { taskId: string }): null {
  return null;
}
