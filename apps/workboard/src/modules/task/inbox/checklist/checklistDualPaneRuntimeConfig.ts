/**
 * PHASE_CHECKLIST_10_DUAL_PANE_RUNTIME — feature gate (UI-only).
 */

/** Dual-pane sync is active in Work Inbox focus runtime (checklist navigator + right detail). */
export function isChecklistDualPaneRuntimeEnabled(): boolean {
  return true;
}
