/**
 * Semantic marker for Focus Runtime — actual footer is AppShell `RuntimeStatusBar`.
 * Tests assert `data-cbv-panel="bottom-runtime-status-bar"` on the focus runtime anchor.
 */
export const BOTTOM_RUNTIME_STATUS_BAR_PANEL = 'bottom-runtime-status-bar';
