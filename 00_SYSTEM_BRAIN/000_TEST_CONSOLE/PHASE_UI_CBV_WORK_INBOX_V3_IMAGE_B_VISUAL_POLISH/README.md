# PHASE_UI_CBV_WORK_INBOX_V3_IMAGE_B_VISUAL_POLISH

**Checks:** `apps/workboard/src/modules/task/inbox/focusRuntimeImageBVisualPolishChecks.ts`

```bash
cd apps/workboard && npm run typecheck && npm run build
```

Manual: 1366×768 on `/inbox` — title, badges, 2-col cards, inline action bar, right tabs, next task top visible.
